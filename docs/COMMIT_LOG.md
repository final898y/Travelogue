# 📝 Travelogue 開發日誌 (Commit Log)

> 記錄專案的開發進程、每次提交的內容與改動方向。

---

## 📅 提交歷史

### [2026-02-16]

#### `pending` - feat(ui): 實作首頁 UI/UX 設計與設計系統優化

- **改動方向**: 建立 Travelogue 的核心視覺風格與首頁列表。
- **具體內容**:
  - 導入 `ui-ux-pro-max` 專家系統，定義「Motion-Driven」風格與「Storytelling」佈局。
  - 新增 `TripCard.vue`: 手帳風格旅程卡片，具備懸停動畫與 SVG 狀態標籤。
  - 新增 `BottomNav.vue`: 行動優先的懸浮導覽列，整合 Lucide 風格 SVG 圖示與毛玻璃效果。
  - 更新 `App.vue`: 實作完整首頁佈局，包含品牌頁首、個人化歡迎語、旅程列表與快速新增區塊。
  - 優化設計系統：在 `App.vue` 引入 Google Fonts 並定義全域字體變數。

### [2026-02-15]

#### `pending` - docs: 更新 Agent 規範並建立 README.md

- **改動方向**: 強化開發流程約束與專案文件。
- **具體內容**:
  - 更新 `AGENTS.md`：加入強制驗證流程（Test -> Lint -> Prettier）、文件同步機制與指令腳本表。
  - 建立 `README.md`：提供專案概述、技術棧與開發指南。

#### `pending` - test: 配置 Vitest 測試環境與編寫範例測試

- **改動方向**: 建立自動化測試基礎。
- **具體內容**:
  - 安裝 `@vue/test-utils` 與 `jsdom`。
  - 更新 `vite.config.ts` 支援 Vitest。
  - 在 `package.json` 加入 `test` 腳本。
  - 新增 `src/components/__tests__/HelloWorld.spec.ts` 範例測試。

#### `6d233b0` - chore: 配置 ESLint 與 Prettier 並更新專案 Script

- **改動方向**: 基礎設施建設與代碼規範自動化。
- **具體內容**:
  - 新增 `eslint.config.js`（含全域排除規則）。
  - 新增 `.prettierrc` 與 `.prettierignore`。
  - 在 `package.json` 加入 `lint`, `lint:fix`, `format`, `format:check` 腳本。
  - 執行全專案自動格式化。

#### `de5bd87` - style: 實作設計系統規範並配置 Tailwind CSS v4

- **改動方向**: 視覺基礎與品牌風格定義。
- **具體內容**:
  - 根據 `docs/04_DESIGN_SYSTEM.md` 配置 Tailwind v4 `@theme`。
  - 加入自定義顏色（Forest Green, Cream, Earth Brown）、圓角與軟陰影（Soft Shadow）。
  - 在 `index.html` 引入 Google Fonts (Noto Sans TC, Varela Round)。
  - 更新 `src/App.vue` 展示設計系統 UI 元件。

#### `dfea005` - feat: 專案初始化

- **改動方向**: 建立專案骨架。
- **具體內容**: 使用 `vite@latest` 建立 Vue 3 + TypeScript 專案。

---

## 🚀 下一步計畫 (Upcoming)

- [ ] 安裝核心生態系套件 (Vue Router, Pinia, Firebase, VueUse)。
- [ ] 建立專案資料夾結構 (Views, Stores, Composables, Services)。
- [ ] 實作 Phase 0 的共用 UI 元件庫。
